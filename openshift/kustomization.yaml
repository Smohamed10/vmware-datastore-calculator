apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - route.yaml
  - networkpolicy.yaml

# Pin the promoted, ACS/Clair-clean image by digest for immutable deploys:
# images:
#   - name: quay.io/YOUR_ORG/vcapacity
#     newName: quay.io/YOUR_ORG/vcapacity
#     digest: sha256:<promoted-digest>
